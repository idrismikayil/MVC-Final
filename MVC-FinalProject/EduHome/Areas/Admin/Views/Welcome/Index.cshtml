@model List<WelcomeMessage>
@{
    ViewData["Title"] = "Index";
}

      <div class="main-panel">
        <div class="content-wrapper">
          <div class="row">
            <div class="col-lg-12 stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Welcome Message</h4>
                  
                  <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                      <thead>
                        <tr>
                          <th>
                            Id
                          </th>
                          <th>
                             Image
                          </th>
                          <th>
                             Title
                          </th>
                          @*<th>
                             Author Name
                          </th>*@
                          <th>
                            Actions
                          </th>
                        </tr>
                      </thead>
                      <tbody>

                       @foreach (var w in Model)
                      {
                           <tr class="table-primary">
                          <td>
                            @w.Id
                          </td>
                          <td>
                            <img src="~/assets/img/about/@w.Image" style="width: 200px; height:200px" />
                          </td>
                          <td>
                              @w.Title
                          </td>
                         @*<td>
                             @blog.AuthorName
                          </td>*@
                          <td>
                         <a class="btn btn-primary" asp-action="Detail" asp-route-id="@w.Id">Detail</a>
                          <a class="btn btn-warning" asp-action="Update" asp-route-id="@w.Id">Update</a>
                          <a class="btn btn-danger "asp-action="Delete" asp-route-id="@w.Id">Delete</a>
                          </td>
                        </tr>
                      }
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>